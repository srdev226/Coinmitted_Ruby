require 'rails_helper'

RSpec.describe "WeekPercentages", type: :request do
  describe "GET /week_percentages" do
    #it "works! (now write some real specs)" do
      #get week_percentages_path
      #expect(response).to have_http_status(200)
    #end
  end
end
