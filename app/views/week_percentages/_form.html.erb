<%= form_with(model: week_percentage, local: true) do |form| %>
  <% if week_percentage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(week_percentage.errors.count, "error") %> prohibited this week_percentage from being saved:</h2>

      <ul>
      <% week_percentage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-group form-group" id="DateDemo">
    <%= form.text_field :date, class: "form-control", id: "weeklyDatePicker", placeholder: "Select Week", autocomplete: "off" %>
  </div>

  <div class="form-group">
    <%= form.label :percentage %>
    <%= form.text_field :percentage, class: "form-control", placeholder: "10" %>
  </div>

  <div class="actions">
    <%= form.submit "Save", class: "btn btn-primary" %>
  </div>
<% end %>
